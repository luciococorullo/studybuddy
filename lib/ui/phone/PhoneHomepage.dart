import 'package:flutter/material.dart';

class PhoneHomepage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Sn un telefono"),
    );
  }
}
